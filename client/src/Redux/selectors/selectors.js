export const cache = ({ publicReducer: { caches = {} } }) => caches;
export const settings = ({ publicReducer: { settings = [] } }) => settings;
export const artifacts = ({ publicReducer: { artifacts = [] } }) => artifacts;
